pipeline {
    agent {
        label "slave-1"
    }
    
    parameters {
        string(defaultValue: "", description: '', name: 'VERSION')
    }

    stages {
        stage('PrintParameter'){
            steps{
                sh 'echo ${VERSION}'
            }
        }
    }
}
